<%= form_for @supplier do |f| %>
    <% if @supplier.errors.any? %>
        <div class="alert alert-danger">
        <strong>
          <%= @supplier.errors.count %> Fehler
          <% if @supplier.errors.count == 1 %>ist<% else %>sind<% end %> aufgetreten:
        </strong>
          <ul>
            <% @supplier.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="panel panel-primary">
      <div class="panel-heading">
        <strong><%= t('supplier') %></strong>
      </div>
      <div class="panel-body">
        <div class="form-group">
          <%= f.label :title %>
          <%= f.text_field :title, :class => "form-control", required: true, autofocus: true %>
        </div>
      </div>
    </div>

    <p>
      <%= f.submit :value => t("save"), :class => "btn btn-primary" %>
      <%= link_to t('back'), suppliers_path, :class => "btn btn-default" %>
    </p>
<% end %>
