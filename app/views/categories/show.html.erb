<p>
  <strong>Title:</strong>
  <%= @category.title %>
</p>

<p>
  <strong><%= t('attributes') %></strong><br>
  <% @category.addribude.all.each do |c| %>
      <%= c.title %><br>
  <% end %>
</p>
<%= link_to t('edit'), edit_category_path(@category) %> |
<%= link_to t('back'), categories_path %>